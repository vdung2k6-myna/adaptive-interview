# Architecture

## System Overview

The Adaptive Interview Engine follows a layered architecture with clear separation between data access, business logic, and presentation layers.

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Pages   │  │ Components│  │  Hooks   │  │  Styles  │   │
│  │(Next.js) │  │(React)   │  │(useState)│  │(Tailwind)│   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
└───────┼─────────────┼─────────────┼─────────────┼───────────┘
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                      API LAYER (Next.js)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ /api/    │  │ /api/    │  │ /api/    │  │ /api/    │  │ /api/    │   │
│  │ sessions │  │ messages │  │positions │  │candidates│  │ campaigns│   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │             │             │             │          │
│  ┌────┴────┐   ┌────┴────┐   ┌────┴────┐   ┌────┴────┐   │
│  │ Route   │   │ Route   │   │ Route   │   │ Route   │   │
│  │Handlers │   │Handlers │   │Handlers │   │Handlers │   │
│  └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘   │
└───────┼─────────────┼─────────────┼─────────────┼───────────┘
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ prompts.ts│  │ ollama.ts│  │evaluation│  │embeddings│   │
│  │(Prompt   │  │(Ollama   │  │  .ts     │  │  .ts     │   │
│  │ Builder) │  │ Client)  │  │(Scoring) │  │(Vectors) │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
└───────┼─────────────┼─────────────┼─────────────┼───────────┘
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                          │
┌─────────────────────────────────────────────────────────────┐
│                     DATA ACCESS LAYER                        │
│  ┌──────────┐  ┌──────────┐                               │
│  │   db.ts   │  │ schema.ts │                               │
│  │(Drizzle  │  │(Table    │                               │
│  │  Pool)   │  │Defs)    │                               │
│  └────┬─────┘  └────┬─────┘                               │
└───────┼─────────────┼───────────────────────────────────────┘
        │             │
   ┌────┴────┐   ┌────┴────┐
   │PostgreSQL│   │ pgvector │
   │(Tables)  │   │(Extension│
   └─────────┘   └─────────┘
```

## Key Design Patterns

### 1. Turn-Based Session Model

Interviews are structured as turn-based sessions with a configurable maximum (default 8 turns). Each turn consists of:

1. **Interviewer question** — Generated by Ollama based on position requirements, candidate background, and conversation history
2. **Candidate answer** — Typed by the user in the chat UI

The session automatically completes when `currentTurn >= maxTurns`.

### 2. Streaming Architecture

The interviewer response streams token-by-token using Ollama's `stream: true` mode:

```
Ollama → NDJSON chunks → Next.js ReadableStream → Client EventSource/fetch reader
```

On the client, chunks are batched to React state updates every ~50ms to prevent excessive re-renders.

### 3. Vector-Based Topic Tracking

Position requirements are embedded (vectorized) when a position is created. During the interview, each message is also embedded. A cosine similarity query determines which requirements have been "covered" by the conversation.

This drives the adaptive questioning — the prompt includes "Topics already covered" and "Remaining topics to explore" sections.

### 4. Evaluation Pipeline

After an interview completes, a separate evaluation process:

1. Fetches the full transcript
2. Builds an evaluation prompt with position requirements and candidate background
3. Sends to Ollama with `temperature: 0.3` for consistency
4. Parses structured JSON response (4 dimension scores + strengths/weaknesses + recommendation)
5. Stores results in the `evaluations` table

## Data Flow: Interview Session

```
User clicks "Start Interview"
    │
    ▼
POST /api/sessions ──→ Create session row (status: "created")
    │
    ▼
Redirect to /interview/{id}
    │
    ▼
GET /api/sessions/{id} ──→ Load session + candidate + position
    │
    ▼
If messages.length === 0:
    POST /api/messages (no content) ──→ Trigger first question
        │
        ▼
    buildPrompt() ──→ Assemble Ollama context
        │
        ▼
    generateChatResponseStream() ──→ Call Ollama /api/chat (stream: true)
        │
        ▼
    Return ReadableStream ──→ Client receives NDJSON chunks
        │
        ▼
    Client batches updates every 50ms ──→ Render with MarkdownRenderer
        │
        ▼
    Stream completes ──→ Server persists full message to DB
        │
    currentTurn increments
    │
User types answer ──→ POST /api/messages (with content)
    │
    ▼
    (Same pipeline generates next question)
    │
After maxTurns:
    Session status → "completed"
    Evaluation becomes available
```

## File Organization

```
src/
├── app/                          # Next.js App Router
│   ├── api/                      # Route handlers
│   │   ├── candidates/           # POST /api/candidates
│   │   │   └── [id]/               # GET/PATCH/DELETE /api/candidates/:id
│   │   ├── evaluations/            # GET /api/evaluations/:id
│   │   ├── messages/             # POST /api/messages
│   │   ├── positions/            # POST /api/positions
│   │   │   └── [id]/               # GET/PATCH/DELETE /api/positions/:id
│   │   ├── sessions/             # GET/POST /api/sessions
│   │   └── campaigns/            # POST /api/campaigns
│   │       └── [id]/
│   │           ├── route.ts        # GET/PATCH/DELETE /api/campaigns/:id
│   │           └── positions/      # POST/DELETE /api/campaigns/:id/positions
│   │       └── [id]/
│   │           ├── route.ts        # GET /api/sessions/:id
│   │           └── evaluate/       # POST /api/sessions/:id/evaluate
│   ├── candidates/               # Candidate list page
│   ├── candidates/new/           # Candidate creation form
│   │   └── [id]/                   # Candidate edit form
│   ├── compare/                  # Side-by-side candidate comparison
│   ├── dashboard/                # Recruiter dashboard
│   ├── interview/[id]/           # Live interview chat
│   │   └── transcript/           # Post-interview review
│   ├── positions/                # Position list page
│   ├── positions/new/          # Position creation form
│   │   └── [id]/                   # Position edit form
│   ├── campaigns/                # Campaign list page
│   ├── campaigns/new/            # Campaign creation form
│   │   └── [id]/                   # Campaign detail + report page
│   ├── setup/                    # Interview setup (select position + candidate)
│   ├── error.tsx                 # Global error boundary
│   ├── globals.css               # Global styles + Markdown theme
│   ├── layout.tsx                # Root layout with nav + fonts
│   └── page.tsx                  # Landing → redirect to dashboard
├── components/                   # Shared React components
│   ├── DeleteButton.tsx          # Client delete button with confirmation
│   └── MarkdownRenderer.tsx      # Rich Markdown rendering with syntax highlighting
└── lib/                          # Business logic and utilities
    ├── config/                   # Environment-specific configuration
    │   ├── index.ts                # Active config export
    │   ├── development.ts          # Dev settings (small pools, short timeouts)
    │   └── production.ts           # Prod settings (large pools, long timeouts)
    ├── db.ts                     # Drizzle database connection
    ├── embeddings.ts             # Vector storage and similarity queries
    ├── errors.ts                 # Custom error classes (OllamaError)
    ├── evaluation.ts             # Post-interview AI evaluation
    ├── ollama.ts                 # Ollama API client (blocking + streaming)
    ├── prompts.ts                # Prompt builder with context assembly
    ├── schema.ts                 # Drizzle table definitions
    └── seed.ts                   # Database seed script
```

## Important Conventions

- **API routes** use `route.ts` files in App Router convention
- **Client components** explicitly declare `"use client"`
- **Server components** are the default (no directive needed)
- **Database queries** use Drizzle's type-safe query builder
- **Raw SQL** is used only for vector similarity queries (Drizzle doesn't support pgvector natively)
- **Error handling** uses custom `OllamaError` class with HTTP status codes
- **Environment config** is centralized in `src/lib/config/` — never hardcode timeouts, pool sizes, or model names outside this layer. Environment variables override config defaults.
